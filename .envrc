# nix-direnv: devShell の評価結果をキャッシュしてリロードを高速化
nix_direnv_version 3

# bun install 済みならローカル node_modules/.bin (tsc など) を PATH に追加
if [[ -d apps/life/node_modules/.bin ]]; then
  PATH_add apps/life/node_modules/.bin
fi

use flake
