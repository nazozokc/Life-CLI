## 開発ルール・方針

### 使用言語

* 実装言語は **TypeScript** を使用してください。

### AI生成コードについて

* AIが生成したコードを利用しても問題ありません。
* ただし、コードの細かい実装をすべて説明できる必要はありませんが、**「何を実装したのか」「どのような動作をするのか」「どのような構成になっているのか」** は自分の言葉で説明できる状態にしてください。
* 保守やデバッグを考慮し、内容を理解しながら実装を進めてください。

### データの保存形式

データを保存する場合は、以下の形式のいずれかを使用してください。

* `json`
* `jsonl`
* `md`
* `yaml`
* `toml`

必要に応じて用途に最も適した形式を選択してください。

### コーディング方針

私が目指している設計思想は次のとおりです。

* **コードはシンプルに保つ**
* **機能はできるだけ充実させる**
* 可読性・保守性を重視する
* 必要以上に複雑な設計や過度な抽象化は避ける
* 将来的な機能追加を考慮し、拡張しやすい構成を意識する

### コマンド追加について

新しいコマンドを実装する場合は、以下のルールに従ってください。

* コマンドは `apps/life/src` 以下に追加してください。
* `index.ts` にコマンドを登録し、CLIから実行できる状態にしてください。
* 可能であれば、コマンドごとにファイルを分割し、`index.ts` では `import` して登録する構成にしてください。
* ファイル分割を行う際は、責務を明確にし、保守しやすいディレクトリ構成を意識してください。

### 実装時の考え方

* シンプルさを最優先にしてください。
* 既存コードの設計や命名規則に合わせて実装してください。
* 不要な依存関係やライブラリの追加は避けてください。
* 将来的な拡張性を考慮しつつ、現在必要な機能を過不足なく実装してください。
* 実装後は、コードだけでなく「何を追加・変更したのか」が分かる構成を意識してください。
