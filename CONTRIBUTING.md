# 開発ルール・方針

## 使用言語

- 実装言語は **TypeScript** を使用してください。

## AI生成コードについて

- AIが生成したコードを利用しても問題ありません。
- ただし、コードの細かい実装をすべて説明できる必要はありません。
- **「何を実装したのか」「どのような動作をするのか」「どのような構成になっているのか」** は自分の言葉で説明できる状態にしてください。
- 保守やデバッグを考慮し、実装内容を理解しながら開発してください。

## データの保存形式

データを保存する場合は、以下のいずれかの形式を使用してください。

- `json`
- `jsonl`
- `md`
- `yaml`
- `toml`

用途に応じて最適な形式を選択してください。

## コーディング方針

このプロジェクトでは、以下の思想を重視しています。

- **コードはシンプル**
- **機能は充実**
- 可読性・保守性を重視する
- 過度な抽象化や不要な設計は避ける
- 将来的な機能追加・変更がしやすい構成にする
- 既存の設計・命名規則に合わせて実装する

## 開発環境

このプロジェクトでは **Nix DevShell** を利用して開発環境を統一しています。

- 開発に必要なツールは可能な限り **DevShell** に追加してください。
- システムへ直接ツールをインストールする前提ではなく、DevShell上で利用できるようにしてください。
- 特別な理由がない限り、開発・ビルド・テストは DevShell 内で行ってください。

また、**direnv** を利用して DevShell を自動で読み込む構成を採用しています。

- リポジトリへ移動した際に `direnv` により DevShell が自動で有効になることを前提としてください。
- 新しく環境変数やツールを追加する場合は、Nix側の設定を更新してください。
- ローカル環境固有の設定に依存しない実装を心がけてください。

## コマンド追加について

新しいコマンドを追加する場合は、以下のルールに従ってください。

- コマンドは `apps/life/src` 以下へ追加してください。
- `index.ts` にコマンドを登録し、CLIから実行できる状態にしてください。
- 可能であれば、コマンドごとにファイルを分割してください。
- `index.ts` は各コマンドを `import` して登録する役割とし、責務を明確にしてください。
- 既存のディレクトリ構成・設計方針を維持してください。

## ライブラリ・依存関係

- 新しいライブラリを追加する場合は、本当に必要か検討してください。
- 標準ライブラリや既存の依存関係で実現できる場合は、それらを優先してください。
- 依存関係を追加した理由が明確であることを意識してください。

## 実装後

実装が完了したら、以下を簡潔に説明してください。

- 何を実装したか
- どのような動作をするか
- 追加・変更したファイル
- 必要に応じて、今後改善できる点や注意点

コードだけでなく、実装内容が第三者にも分かる状態を目指してください。
